# Release Notes — v0.6.0 (Milestone 2.5: Image Intelligence)

**Released:** 2026-08-04
**Milestone:** Phase 2, Milestone 2.5 — see `docs/PHASE_2_MILESTONE_2_5_ENGINEERING_SPECIFICATION.md` (v1.3 + remediation review R1–R3)

---

## What's New

- **Image metadata models** — `ImageInfo` / `ImageExif` pydantic models (additive, `extra="forbid"`, nullable `page_no`, `index`, EXIF raw+decoded, optional GPS) in `app/domain/document_intelligence.py`. (P2-501)
- **EXIF/metadata extractor** — `images/metadata.py` `ImageAnalyzer`/`analyze_image` (single owner of raw EXIF read, R-3); `ImageIngestor` attaches `metadata.extra["image_info"]` for image kinds, gated by `intelligence.images.exif_enabled`. Corrupt/unreadable EXIF → empty `ImageExif(raw={}, decoded={})`, no crash. (P2-502)
- **Diagram intelligence** — `images/diagram.py` (`drawio_to_mermaid`, `DiagramParser`); `DiagramProcessor` emits Mermaid note / raw fallback, gated by `intelligence.images.diagram_enabled`. `.drawio` → Mermaid skeleton (fixed-fixture comparison); `.mmd` passthrough; parse failure → raw text fallback. (P2-504)
- **Configurable processor prompts + `{language}` wiring** — OCR/vision/handwriting prompts come from `intelligence.prompts.*` with the `{language}` slot substituted at the processor call site (P2-205 delta, R-6). (P2-505)
- **Multi-image (PDF-with-images) enrichment** — self-contained `_enrich_images` at the shared P2-305 call site, triggered by the existing `kind == "pdf"` classifier condition; per-embedded-image `ImageInfo` dumps land in `metadata.extra["images"]` with `page_no`/`index` provenance; coexists with the table gate (M2.4 R2 precedent). (P2-506)
- **Preprocessing now actually wired into OCR** — the shared `imaging/preprocess.py` `Preprocessor` is bridged into both engines (`ocr/__init__.py` `_shared_preprocessor`); the bridge is only built when at least one preprocess toggle (`ocr.preprocess` or `images.preprocess`) is enabled. All three processors carry an explicit `preprocess` kwarg driven by per-path config: `VisionProcessor` consumes `intelligence.images.preprocess`; `OCRProcessor` and `HandwritingProcessor` consume `intelligence.ocr.preprocess`. The `_extract_via_service` helper and `DocumentOcrService.extract` / `OcrEngine.run` defaults are `False` — no production path enables preprocessing without config. (R-a, review remediation, re-review)

---

## Behavior Changes

- **`intelligence.images` config** aligned to the frozen §4.5 contract: `max_dimensions: [8192, 8192]` (scalar max-edge int or `[width, height]` pair both accepted) and `max_bytes: 20 MB` (20,971,520) now supersede the historical `MAX_EDGE = 8000` / 25 MiB defaults. `ImageSettings.max_dimensions` is `int | tuple[int, int]`; invalid values are rejected at parse time. (review remediation)
- All three OCR/vision processors route through `_extract_via_service(..., preprocess=<config>)` — the single call site that hands the per-path config-driven `preprocess` flag to `DocumentOcrService.extract()`. The `_shared_preprocessor` bridge returns `None` when both toggles are off, preventing any unconfigured preprocessing.
- Protocol defaults: `OcrEngine.run(preprocess: bool = False)`, `DocumentOcrService.extract(preprocess: bool = False)` — preprocess is opt-in, never a config-bypassing default.
- Rollback contract: `intelligence.images.exif_enabled: false` ⇒ no `image_info` key; `diagram_enabled: false` ⇒ `.drawio` raw text passthrough; `preprocess: false` ⇒ no preprocessing — all Phase-1-identical. (R-4)

---

## Requirements

- **Core (existing):** `pymupdf` — required for scanned-PDF OCR rendering (clear `ImportError` if absent, G06)
- **Optional (`intelligence` extra, existing):** `pillow>=10.0.0`, `numpy>=1.24.0` — for preprocessing pipeline; when absent, preprocessing skips with logged warning (C-3)
- **Optional (existing):** `pytesseract` + Tesseract binary — for offline OCR fallback

---

## Known Issues

- **Per-task atomic commits not yet made (spec §14):** milestone work uncommitted; commit per-task atomic commits before release (same status as M2.2/M2.3/M2.4).
- **62 pre-existing ruff errors and 3 pre-existing mypy errors** remain in files untouched by M2.5 (docx/pptx/spreadsheet ingestors, vision_client, whisper_transcriber, queue worker, templates, search, etc.). They predate this milestone and are not part of its gate.
- **PDF-only extraction trigger:** per the frozen spec, embedded-image extraction fires on `kind == "pdf"` (text-bearing PDFs). Pure image PDFs classify as `scanned_pdf` and are intentionally skipped by the gate (no invented routing conditions) — their OCR path is unchanged.

---

## Verification

- 825 tests pass / 28 deselected (integration-gated); coverage 88% (floor 80%); M2.5 modules: `images/metadata.py` 89%, `images/diagram.py` 91%, `images/multi.py` 80%, `imaging/preprocess.py` 95%, `image_ingestor.py` 93%, `document_intelligence.py` 100%
- New unit suite `tests/unit/test_image_intelligence.py` (40 tests) covers: models (3), analyzer (7), ingestor (3), multi (5), preprocess (4), diagram (6), diagram-processor (3), language wiring (4), `_enrich_images` (5)
- New integration suite `tests/integration/test_image_pipeline.py` (4 tests, `-m integration`) covers: photo→`image_info` + vision prompt; no-vision passthrough; PDF-with-image page provenance; text-only PDF has no `"images"` key
- Ruff: 0 new errors in changed files (3 auto-fixable import issues in new test file; 62 pre-existing errors remain in untouched files)
- Mypy: 0 new type errors in changed files (3 pre-existing errors — pptx, faster_whisper, numpy — in untouched files)
- Absent-Pillow DoD (C-3): `ImageAnalyzer` degrades to file-level info (Pillow optional); preprocess skips with logged warning
- Absent-PyMuPDF DoD: `MultiImageExtractor` returns `[]` with logged warning; workflow leaves document unchanged
- Rollback (R-4): `exif_enabled: false` → no `"image_info"` key (proven live); `diagram_enabled: false` → `DiagramProcessor` passthrough (unit-tested); `preprocess` defaults `false` → Phase-1 behavior

---

## Rollback

Set `intelligence.images.exif_enabled: false` — no `metadata.extra["image_info"]` key is written.
Set `intelligence.images.diagram_enabled: false` — `.drawio` files pass through as raw text.
Set `intelligence.images.preprocess: false` — no preprocessing occurs (default behavior).

All three toggles restore Phase-1-identical behavior (additive schemas only, no migration).

---

## See Also

- `docs/PHASE_2_MILESTONE_2_5_COMPLETION_REPORT.md`
- `docs/PHASE_2_MILESTONE_2_5_DOCUMENTATION_SYNCHRONIZATION_REPORT.md`
- `docs/PHASE_2_MILESTONE_2_5_IMPLEMENTATION_REPORT.md`
- `docs/PHASE_2_MILESTONE_2_5_REMEDIATION_REPORT.md`